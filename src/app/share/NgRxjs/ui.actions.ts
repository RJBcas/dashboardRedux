import { createAction } from '@ngrx/store';

export const isLoading = createAction('[TODO] Is Loading')
export const stopLoadin = createAction('[TODO] Stop Loading')

/* export const increment = createAction('[Counter Component] Increment'); */
